import React from 'react'

export default function ProceduresCard({ data }: { key: number; data: { id: number; title: string; description: string; }; }) {

    console.log(data.id);

    return (
        <div></div>
    )
}
